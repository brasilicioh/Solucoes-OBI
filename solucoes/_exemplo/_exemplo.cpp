// Exemplo de uso do repositório: soma de dois inteiros
// teste com: "python validar.py _exemplo.cpp" no terminal

#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n1, n2;
    cin >> n1 >> n2;

    cout << n1+n2 << "\n";
}